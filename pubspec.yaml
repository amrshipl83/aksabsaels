name: aksabsales
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # =========================================================
  # 🎯 المكتبات الضرورية لصفحة تسجيل الدخول (Firebase + Local Storage)
  # =========================================================
  firebase_core: ^3.2.0        # لإدارة وإعداد تطبيق Firebase
  firebase_auth: ^5.1.4         # للمصادقة وتسجيل الدخول
  cloud_firestore: ^5.1.0       # للبحث عن دور المستخدم وحالته
  shared_preferences: ^2.2.3    # لحفظ بيانات المستخدم ودوره محلياً
  # =========================================================

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
